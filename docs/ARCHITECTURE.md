# ARCHITECTURE PROJET PLAN & CO

## 📁 ARBORESCENCE FRONTEND (Angular 20)

```
frontend/
│
├── src/
│   ├── app/
│   │   ├── core/                          # Services globaux, guards, interceptors
│   │   │   ├── guards/
│   │   │   │   └── auth.guard.ts          # Protection route /admin
│   │   │   ├── interceptors/
│   │   │   │   └── auth.interceptor.ts    # Injection JWT dans headers
│   │   │   └── services/
│   │   │       ├── auth.service.ts        # Auth JWT (login, logout, isAuth)
│   │   │       ├── contact.service.ts     # API contact (POST /api/contact)
│   │   │       ├── admin.service.ts       # API admin (GET /api/contacts)
│   │   │       └── scroll-spy.service.ts  # Détection section visible
│   │   │
│   │   ├── shared/                        # Composants réutilisables
│   │   │   ├── components/
│   │   │   │   ├── navbar/
│   │   │   │   │   ├── navbar.ts
│   │   │   │   │   ├── navbar.html
│   │   │   │   │   └── navbar.scss
│   │   │   │   └── footer/
│   │   │   │       ├── footer.ts
│   │   │   │       ├── footer.html
│   │   │   │       └── footer.scss
│   │   │   └── models/
│   │   │       ├── contact.interface.ts   # Interface formulaire contact
│   │   │       └── admin.interface.ts     # Interfaces dashboard admin
│   │   │
│   │   ├── features/                      # Pages/Features organisées
│   │   │   ├── home/
│   │   │   │   ├── components/
│   │   │   │   │   ├── banner/            # Section hero
│   │   │   │   │   │   ├── banner.ts
│   │   │   │   │   │   ├── banner.html
│   │   │   │   │   │   └── banner.scss
│   │   │   │   │   ├── services/          # Grille services
│   │   │   │   │   │   ├── services.ts
│   │   │   │   │   │   ├── services.html
│   │   │   │   │   │   └── services.scss
│   │   │   │   │   ├── tarifs/            # Packages tarifs
│   │   │   │   │   │   ├── tarifs.ts
│   │   │   │   │   │   ├── tarifs.html
│   │   │   │   │   │   └── tarifs.scss
│   │   │   │   │   └── cta/               # Call to action
│   │   │   │   │       ├── cta.ts
│   │   │   │   │       ├── cta.html
│   │   │   │   │       └── cta.scss
│   │   │   │   └── home.ts                # Page container
│   │   │   │       ├── home.html
│   │   │   │       └── home.scss
│   │   │   │
│   │   │   ├── contact/
│   │   │   │   ├── contact.ts             # Page formulaire
│   │   │   │   ├── contact.html
│   │   │   │   └── contact.scss
│   │   │   │
│   │   │   └── admin/
│   │   │       ├── components/
│   │   │       │   ├── login/             # Formulaire login
│   │   │       │   │   ├── login.ts
│   │   │       │   │   ├── login.html
│   │   │       │   │   └── login.scss
│   │   │       │   └── dashboard/         # Liste contacts
│   │   │       │       ├── dashboard.ts
│   │   │       │       ├── dashboard.html
│   │   │       │       └── dashboard.scss
│   │   │       └── admin.ts               # Container admin
│   │   │           ├── admin.html
│   │   │           └── admin.scss
│   │   │
│   │   ├── app.routes.ts                  # Configuration routes
│   │   ├── app.config.ts                  # Configuration app
│   │   └── app.component.ts               # Root component
│   │       ├── app.component.html
│   │       └── app.component.scss
│   │
│   ├── environments/
│   │   ├── environment.ts                 # Config dev
│   │   └── environment.prod.ts            # Config prod
│   │
│   ├── assets/
│   │   ├── images/                        # Images du site
│   │   └── styles/                        # Styles globaux
│   │       ├── _variables.scss            # Variables (couleurs, spacing)
│   │       ├── _mixins.scss               # Mixins réutilisables
│   │       └── styles.scss                # Import global
│   │
│   ├── index.html
│   ├── main.ts
│   └── styles.scss
│
├── angular.json
├── package.json
├── tsconfig.json
└── README.md
```

---

## 📁 ARBORESCENCE BACKEND (Express)

```
backend/
│
├── src/
│   ├── config/
│   │   ├── database.ts                    # Config Supabase
│   │   └── email.ts                       # Config service email
│   │
│   ├── middleware/
│   │   ├── auth.middleware.ts             # Vérification JWT
│   │   └── validation.middleware.ts       # Validation données
│   │
│   ├── routes/
│   │   ├── contact.routes.ts              # POST /api/contact
│   │   └── admin.routes.ts                # GET /api/contacts, PATCH /api/contacts/:id
│   │
│   ├── controllers/
│   │   ├── contact.controller.ts          # Logique contact
│   │   └── admin.controller.ts            # Logique admin
│   │
│   ├── services/
│   │   ├── contact.service.ts             # Sauvegarde Supabase + email
│   │   ├── auth.service.ts                # Génération JWT
│   │   └── email.service.ts               # Envoi emails
│   │
│   ├── models/
│   │   └── contact.interface.ts           # Types TypeScript
│   │
│   ├── utils/
│   │   └── validators.ts                  # Validations custom
│   │
│   ├── app.ts                             # Config Express
│   └── server.ts                          # Point d'entrée
│
├── .env                                   # Variables env (git-ignored)
├── .env.example                           # Template variables
├── package.json
├── tsconfig.json
└── README.md
```

---

## 🗺️ SCHÉMA NAVIGATION (Routes Angular)

```
┌─────────────────────────────────────────────────────────────┐
│                    APP COMPONENT                            │
│                 <app-navbar />                              │
│                 <router-outlet />                           │
│                 <app-footer />                              │
└─────────────────────────────────────────────────────────────┘
                          │
          ┌───────────────┼───────────────┐
          │               │               │
          ▼               ▼               ▼
     ┌─────────┐    ┌──────────┐   ┌──────────┐
     │  HOME   │    │ CONTACT  │   │  ADMIN   │
     │   (/)   │    │(/contact)│   │ (/admin) │
     └─────────┘    └──────────┘   └──────────┘
          │                              │
          │                         [Auth Guard]
          │                              │
          ▼                              ▼
┌──────────────────┐            ┌────────────────┐
│  Single Page     │            │  Login         │
│  avec Scroll     │            │  (/admin/login)│
└──────────────────┘            └────────────────┘
     │                                   │
     │                              [JWT Valid?]
     ▼                                   │
┌─────────────┐                          ▼
│ #hero       │                  ┌────────────────┐
│ #services   │                  │  Dashboard     │
│ #tarifs     │                  │(/admin/dashboard)
│ #cta        │                  └────────────────┘
└─────────────┘
```

---

## 🔄 FLOW SCROLL SPY (Navigation Home)

```
┌────────────────────────────────────────────────────────────────┐
│                         NAVBAR                                 │
│  [Accueil] [Services] [Tarifs] [Contact]                      │
└────────────────────────────────────────────────────────────────┘
                           │
                           │ IntersectionObserver
                           │ détecte section visible
                           ▼
┌────────────────────────────────────────────────────────────────┐
│                     HOME COMPONENT (/)                         │
│                                                                │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ <section id="hero">                                  │    │
│  │   <app-banner />                                     │    │
│  │ </section>                                           │    │
│  └──────────────────────────────────────────────────────┘    │
│         │                                                      │
│         │ Scroll ↓                                            │
│         ▼                                                      │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ <section id="services">                              │    │
│  │   <app-services />                                   │ ◄──┼─ Active navbar "Services"
│  │ </section>                                           │    │
│  └──────────────────────────────────────────────────────┘    │
│         │                                                      │
│         │ Scroll ↓                                            │
│         ▼                                                      │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ <section id="tarifs">                                │    │
│  │   <app-tarifs />                                     │ ◄──┼─ Active navbar "Tarifs"
│  │ </section>                                           │    │
│  └──────────────────────────────────────────────────────┘    │
│         │                                                      │
│         │ Scroll ↓                                            │
│         ▼                                                      │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ <section id="cta">                                   │    │
│  │   <app-cta />                                        │    │
│  │ </section>                                           │    │
│  └──────────────────────────────────────────────────────┘    │
│                                                                │
└────────────────────────────────────────────────────────────────┘

ScrollSpyService:
1. IntersectionObserver sur chaque <section>
2. Détecte section >= 50% visible
3. Update Router avec fragment correspondant
4. RouterLinkActive auto-active le lien navbar
```

---

## 🔐 FLOW AUTHENTIFICATION ADMIN

```
┌──────────────────────────────────────────────────────────────┐
│ User visite /admin                                           │
└──────────────────────────────────────────────────────────────┘
                           │
                           │ AuthGuard check
                           ▼
                    ┌─────────────┐
                    │ JWT valide? │
                    └─────────────┘
                      │         │
              NON ◄───┘         └───► OUI
               │                      │
               ▼                      ▼
    ┌─────────────────┐    ┌──────────────────┐
    │ Redirect        │    │ Afficher         │
    │ /admin/login    │    │ Dashboard        │
    └─────────────────┘    └──────────────────┘
               │
               ▼
    ┌─────────────────┐
    │ Formulaire      │
    │ Username + Pass │
    └─────────────────┘
               │
               │ Submit
               ▼
    ┌─────────────────────────────┐
    │ AuthService.login()         │
    │ POST /api/admin/login       │
    └─────────────────────────────┘
               │
               │ Backend vérifie
               ▼
    ┌─────────────────────────────┐
    │ Retour JWT token            │
    │ Stockage localStorage       │
    └─────────────────────────────┘
               │
               │ Redirect
               ▼
    ┌─────────────────────────────┐
    │ Dashboard /admin/dashboard  │
    │ Liste contacts              │
    └─────────────────────────────┘
```

---

## 📨 FLOW FORMULAIRE CONTACT

```
┌─────────────────────────────────────────────────────────────┐
│ User remplit formulaire /contact                            │
│ (Nom, Email, Téléphone, Message)                            │
└─────────────────────────────────────────────────────────────┘
                           │
                           │ Submit
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ ContactService.submit()                                     │
│ POST /api/contact                                           │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ BACKEND (Express)                                           │
│                                                             │
│ 1. Validation données (middleware)                         │
│ 2. Sauvegarde Supabase (table contacts)                    │
│ 3. Envoi email notification admin                          │
│ 4. Retour success/error                                    │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│ FRONTEND                                                    │
│ Affiche message succès                                      │
│ "Merci, je vous réponds sous 24h"                          │
└─────────────────────────────────────────────────────────────┘
```

---

## 🗄️ BASE DE DONNÉES SUPABASE

```
┌───────────────────────────────────────────────────────────┐
│                    Table: contacts                        │
├────────────┬──────────────┬───────────────────────────────┤
│   Column   │     Type     │        Description            │
├────────────┼──────────────┼───────────────────────────────┤
│ id         │ uuid         │ Primary key (auto)            │
│ first_name │ text         │ Prénom                        │
│ last_name  │ text         │ Nom                           │
│ email      │ text         │ Email                         │
│ phone      │ text         │ Téléphone (optionnel)         │
│ company    │ text         │ Entreprise (optionnel)        │
│ service    │ text         │ Service souhaité              │
│ budget     │ text         │ Budget estimé                 │
│ message    │ text         │ Message                       │
│ status     │ text         │ nouveau/lu/traité             │
│ created_at │ timestamp    │ Date création                 │
│ updated_at │ timestamp    │ Date modification             │
└────────────┴──────────────┴───────────────────────────────┘

Index:
- email (unique)
- status (pour filtrage dashboard)
- created_at (tri par date)

RLS (Row Level Security):
- SELECT: Admin seulement (JWT vérifié)
- INSERT: Public (formulaire contact)
- UPDATE: Admin seulement
- DELETE: Admin seulement
```

---

## 🔌 API ENDPOINTS (Backend Express)

```
┌─────────────────────────────────────────────────────────────┐
│                    PUBLIC ROUTES                            │
├─────────────────────────────────────────────────────────────┤
│ POST   /api/contact                                         │
│ Body:  { firstName, lastName, email, phone?,                │
│          company?, service, budget?, message }              │
│ Response: { success: true, message: "Contact enregistré" } │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    ADMIN ROUTES (JWT)                       │
├─────────────────────────────────────────────────────────────┤
│ POST   /api/admin/login                                     │
│ Body:  { username, password }                               │
│ Response: { token: "JWT_TOKEN" }                            │
│                                                             │
│ GET    /api/admin/contacts                                  │
│ Query: ?status=nouveau&page=1&limit=20                     │
│ Headers: Authorization: Bearer JWT_TOKEN                    │
│ Response: { contacts: [...], total: 50, page: 1 }          │
│                                                             │
│ PATCH  /api/admin/contacts/:id                              │
│ Body:  { status: "lu" | "traité" }                         │
│ Headers: Authorization: Bearer JWT_TOKEN                    │
│ Response: { success: true, contact: {...} }                │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎨 STRUCTURE STYLES (SCSS)

```
src/assets/styles/
│
├── _variables.scss          # Couleurs, spacing, fonts
│   └── $primary-color: #E67E22;
│   └── $secondary-color: #F39C12;
│   └── $text-dark: #2C3E50;
│   └── $spacing-md: 1.5rem;
│   └── ...
│
├── _mixins.scss             # Mixins réutilisables
│   └── @mixin button { ... }
│   └── @mixin card { ... }
│   └── @mixin gradient-warm { ... }
│
├── _reset.scss              # Reset CSS
├── _typography.scss         # Fonts, headings
├── _layout.scss             # Container, grid
└── styles.scss              # Import tout

Composants utilisent:
@use '@assets/styles/variables' as *;
@use '@assets/styles/mixins' as *;
```

---

## 🚀 DÉPLOIEMENT

```
┌──────────────────────────────────────────────────────────────┐
│                    DÉVELOPPEMENT                             │
├──────────────────────────────────────────────────────────────┤
│ Frontend: ng serve (localhost:4200)                         │
│ Backend:  npm run dev (localhost:3000)                      │
│ Database: Supabase Cloud                                    │
└──────────────────────────────────────────────────────────────┘
                           │
                           │ Build & Deploy
                           ▼
┌──────────────────────────────────────────────────────────────┐
│                     PRODUCTION                               │
├──────────────────────────────────────────────────────────────┤
│ Frontend: Firebase Hosting                                  │
│   → ng build --configuration production                     │
│   → firebase deploy --only hosting                          │
│   → URL: planandco.web.app                                  │
│                                                              │
│ Backend: Render                                             │
│   → Git push → Auto deploy                                  │
│   → URL: planandco-api.onrender.com                         │
│                                                              │
│ Database: Supabase                                          │
│   → Hébergé par Supabase                                    │
│   → URL: xxx.supabase.co                                    │
└──────────────────────────────────────────────────────────────┘
```

---

## 📊 DIAGRAMME COMPLET (Vue d'ensemble)

```
┌──────────────────────────────────────────────────────────────────────┐
│                          UTILISATEUR                                 │
└──────────────────────────────────────────────────────────────────────┘
                                  │
                                  │ HTTPS
                                  ▼
┌──────────────────────────────────────────────────────────────────────┐
│                   FRONTEND (Firebase Hosting)                        │
│                      Angular 20 Application                          │
│                                                                      │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐   │
│  │   Navbar   │  │    Home    │  │  Contact   │  │   Admin    │   │
│  │ (ScrollSpy)│  │  (Scroll)  │  │ (Formulaire)│  │(Dashboard) │   │
│  └────────────┘  └────────────┘  └────────────┘  └────────────┘   │
│                                                                      │
│  Services Angular:                                                  │
│  - ContactService  → API calls                                      │
│  - AuthService     → JWT management                                 │
│  - AdminService    → Dashboard data                                 │
│  - ScrollSpyService → Section detection                             │
└──────────────────────────────────────────────────────────────────────┘
                                  │
                                  │ HTTP REST API
                                  ▼
┌──────────────────────────────────────────────────────────────────────┐
│                    BACKEND (Render)                                  │
│                 Express + TypeScript API                             │
│                                                                      │
│  Routes:                                                             │
│  - POST   /api/contact          → Nouveau contact                   │
│  - POST   /api/admin/login      → Auth admin                        │
│  - GET    /api/admin/contacts   → Liste contacts                    │
│  - PATCH  /api/admin/contacts/:id → Update statut                   │
│                                                                      │
│  Middleware:                                                         │
│  - auth.middleware.ts  → Vérif JWT                                  │
│  - validation.middleware.ts → Validation données                    │
└──────────────────────────────────────────────────────────────────────┘
                                  │
                    ┌─────────────┴─────────────┐
                    │                           │
                    ▼                           ▼
┌─────────────────────────────┐   ┌─────────────────────────────┐
│   DATABASE (Supabase)       │   │   EMAIL SERVICE             │
│                             │   │   (Resend/SendGrid)         │
│   Table: contacts           │   │                             │
│   - id, name, email...      │   │   Notifications admin       │
│   - status, created_at      │   │   lors nouveau contact      │
│                             │   │                             │
│   RLS activé (sécurité)     │   └─────────────────────────────┘
└─────────────────────────────┘
```

---

## 🎯 POINTS CLÉS ARCHITECTURE

### Séparation des responsabilités
- **Frontend** : UI/UX, validation client, navigation
- **Backend** : Logique métier, validation serveur, sécurité
- **Database** : Persistance données, RLS

### Communication
- Frontend ↔ Backend : REST API (JSON)
- Backend ↔ Database : Supabase Client SDK
- Backend ↔ Email : API service email

### Sécurité
- JWT pour routes admin (HttpOnly si possible)
- CORS configuré (whitelist frontend)
- Validation données côté serveur
- RLS Supabase (protection données)
- Variables sensibles en `.env`

### Performance
- Lazy loading des routes Angular
- Signals pour réactivité
- Optimisation images (WebP, lazy load)
- Minification production (ng build)

### Scalabilité
- Architecture modulaire (features)
- Services injectables réutilisables
- API REST extensible
- Database normalisée

---

**Cette architecture garantit un projet moderne, maintenable et évolutif. 🚀**